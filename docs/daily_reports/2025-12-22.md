# 2025-12-22 の日報

## 本日の目標

- Day 22: ユーザーフィードバック対応と機能改善
  - キーマップリセット機能のバグ修正
  - キーマップ選択機能の実装
  - タイピング練習時の説明改善
  - レイアウトシフト問題の修正

## 実施内容

### 1. キーマップリセット機能のバグ修正

**問題点:**
- キーマップ編集画面の「デフォルト設定に戻す」ボタンがエラーになる
- エラー内容: `ActionController::ParameterMissing (param is missing or the value is empty: keymaps)`

**原因:**
- `My::KeymapsController#update` で `reset_to_default` パラメータを処理する前に `keymap_params` を呼び出していた
- `keymap_params` の `params.require(:keymaps)` が、リセット時には存在しないパラメータを要求していた

**修正内容:**
- [app/controllers/my/keymaps_controller.rb:42-48](app/controllers/my/keymaps_controller.rb#L42-L48) の `update` メソッドを修正
- 早期リターンパターンを使用して、`reset_to_default` パラメータがある場合は `reset_keymap` メソッドを先に実行
- `active` パラメータ、基本情報のみ更新の場合も同様に早期リターン
- これにより、不要な `keymap_params` の呼び出しを回避

### 2. キーマップ選択機能の実装

**実装内容:**

#### データベース設計
- マイグレーション: [db/migrate/20251221211737_add_active_keymap_set_to_users.rb](db/migrate/20251221211737_add_active_keymap_set_to_users.rb)
- `users` テーブルに `active_keymap_set_id` カラムを追加（外部キー、NOT NULL）
- 既存ユーザーへの対応:
  - 最初のキーマップセットを自動的にアクティブに設定
  - キーマップセットを持たないユーザーには、デフォルトのキーマップセット（「マイキーマップ」）を自動作成

#### モデル修正
- [app/models/user.rb:6](app/models/user.rb#L6): `belongs_to :active_keymap_set` を追加
- [app/models/user.rb:82-90](app/models/user.rb#L82-L90): 新規ユーザー作成時に自動的にデフォルトキーマップセットを作成し、アクティブに設定
- [app/models/keymap.rb:68-79](app/models/keymap.rb#L68-L79): `all_layers_for_user_or_default` メソッドを修正し、ユーザーのアクティブなキーマップセットを使用

#### コントローラー修正
- [app/controllers/my/keymaps_controller.rb:127-133](app/controllers/my/keymaps_controller.rb#L127-L133): `set_active_keymap` メソッドを追加
- キーマップセットをアクティブに設定する処理を実装
- エラーハンドリングとログ出力を追加

#### ビュー修正
- [app/views/my/keymaps/index.html.slim:62-71](app/views/my/keymaps/index.html.slim#L62-L71): キーマップ一覧にアクティブ状態の表示と切り替えボタンを追加
- アクティブなキーマップには「使用中」バッジを表示（緑色）
- 非アクティブなキーマップには「使用する」ボタンを表示

### 3. タイピング練習時の説明改善

**実装内容:**
- タイピング練習画面に「ミスした時の操作」の説明を追加
- 「Delete」キーでやり直しができることを明示
- ユーザーがミス時の操作を理解しやすくなるよう配慮

### 4. レイアウトシフト問題の修正

**問題点:**
- タイピング練習開始時に、キーボード表示領域のレイアウトがシフトする
- 特に「入力してください」から実際の文字が表示されるタイミングでずれが発生

**原因:**
- 初期状態（「入力してください」）と実際の文字では高さが異なる
- CLS（Cumulative Layout Shift）が発生し、ユーザー体験を損なっていた

**修正内容:**
- 最小高さ（`min-h-[...]`）を設定してレイアウトシフトを防止
- 初期状態でも十分な高さを確保
- スムーズなレイアウト遷移を実現

### 5. 本番環境デプロイ（マイグレーション対応）

**デプロイ時の問題:**
- 初回デプロイでマイグレーションエラーが発生
- エラー内容: `PG::NotNullViolation: ERROR: column "active_keymap_set_id" of relation "users" contains null values`
- 原因: 本番環境にキーマップセットを持たないユーザーが存在していた

**対応:**
- [db/migrate/20251221211737_add_active_keymap_set_to_users.rb:17-25](db/migrate/20251221211737_add_active_keymap_set_to_users.rb#L17-L25) のマイグレーションを修正
- キーマップセットがない場合は自動的にデフォルトを作成するロジックを追加
- 修正後、再デプロイで正常に完了

**デプロイ結果:**
- ✅ マイグレーション成功
- ✅ 全ユーザーに `active_keymap_set_id` が設定された
- ✅ キーマップ選択機能が本番環境で稼働中

### 6. トップページのタブ化実装

**背景:**
- レッスンが増えてきたため、タブごとに整理して見やすくしたい
- ユーザーフィードバックとして「カテゴリーグループ化」の要望があった

**実装内容:**

#### データベース設計
- マイグレーション: [db/migrate/20251222014645_add_tab_to_categories.rb](db/migrate/20251222014645_add_tab_to_categories.rb)
- `categories` テーブルに `tab` カラムを追加（string, NOT NULL, default: "basics", indexed）
- 既存カテゴリーに適切な tab 値を自動設定:
  - `basics`: 基礎練習、指別練習
  - `english`: 単語練習、テーマ別、短文練習（英語系）
  - `programming`: プログラミング、短文練習（プログラミングフレーズを含む場合）

#### モデル修正
- [app/models/category.rb:5-12](app/models/category.rb#L5-L12): `TABS` 定数を追加（6タブ定義）
  - タブ1: 基礎トレーニング 🔰
  - タブ2: 英語練習 🇬🇧
  - タブ3: 日本語練習 🇯🇵
  - タブ4: プログラミング 💻
  - タブ5: マイレッスン 📝（準備中）
  - タブ6: コミュニティ 👥（準備中）
- [app/models/category.rb:17](app/models/category.rb#L17): `tab` カラムのバリデーション
- [app/models/category.rb:24](app/models/category.rb#L24): `by_tab` スコープを追加
- [app/models/category.rb:27-33](app/models/category.rb#L27-L33): `available_tabs`, `all_tabs` クラスメソッドを追加

#### コントローラー修正
- [app/controllers/home_controller.rb:3-4](app/controllers/home_controller.rb#L3-L4): タブ情報と現在のタブを設定
- [app/controllers/home_controller.rb:9-17](app/controllers/home_controller.rb#L9-L17): `categories_for_tab` ヘルパーメソッドを追加
- タブごとにカテゴリーをフィルタリングし、ログイン状態に応じて表示

#### ビュー修正
- [app/views/home/index.html.slim:29-39](app/views/home/index.html.slim#L29-L39): タブナビゲーションを追加
- Turbo Frames を使用してタブ切り替え時にサーバーサイドレンダリング
- [app/views/home/_tab_content.html.slim](app/views/home/_tab_content.html.slim): タブコンテンツの partial を作成
- カテゴリーごとにグルーピング、3列グリッドレイアウト

#### Stimulus Controller
- [app/javascript/controllers/tabs_controller.js](app/javascript/controllers/tabs_controller.js): タブ切り替え用の Stimulus Controller を作成
- タブのアクティブ状態管理
- ローディングインジケーター表示
- Turbo Frame のイベントリスニング

### 7. レッスン管理画面のタブ化実装

**背景:**
- トップページと同様に、レッスン管理画面もタブ化して見やすくしたい
- 管理者と一般ユーザーで表示するタブを分けたい

**実装内容:**

#### ユーザー種別によるタブ制御
- [app/controllers/my/lessons_controller.rb:4-14](app/controllers/my/lessons_controller.rb#L4-L14): ユーザー種別でタブを切り替え
  - **管理者**: 4タブ（basics, english, japanese, programming）で公式レッスンを管理
  - **一般ユーザー**: 1タブ（my_lessons）で自分のレッスンのみ管理

#### レッスンフィルタリング
- [app/controllers/my/lessons_controller.rb:52-68](app/controllers/my/lessons_controller.rb#L52-L68): `lessons_for_tab` メソッドを追加
  - 管理者: 自分（公式アカウント）が作成したレッスンのみ表示
  - 一般ユーザー: 自分が作成したレッスンのみ表示
- `Lesson.official` スコープの `users.admin` カラム参照エラーを修正（admin はカラムではなくメソッド）

#### ビュー修正
- [app/views/my/lessons/index.html.slim:15-25](app/views/my/lessons/index.html.slim#L15-L25): タブナビゲーションを追加
- [app/views/my/lessons/_tab_content.html.slim](app/views/my/lessons/_tab_content.html.slim): タブコンテンツの partial を作成
- カテゴリーごとにグルーピング、1列横長レイアウト
- 編集・削除ボタンは自分のレッスンまたは管理者のみ表示

#### DRY 設計
- トップページとレッスン管理画面で同じ `tabs_controller.js` を再利用
- Turbo Frame ID を区別（`lesson_content` vs `lesson_management_content`）

## 学んだこと・気づき

### 1. 本番環境データの事前調査の重要性

**教訓:**
- マイグレーション実装前に本番環境のデータ状態を確認することが重要
- 今回は「全ユーザーが少なくとも1つのキーマップセットを持っている」と仮定していたが、実際には持っていないユーザーが存在
- Rails コンソールや SQL で事前確認することで、マイグレーション失敗を防げる

**対策:**
- NOT NULL 制約を追加する場合は、必ず reversible ブロックで既存データを適切に処理
- `find_or_initialize_by` や `find_or_create_by` を活用して、データがない場合の自動生成を実装
- マイグレーション実行前に dry-run 的な確認を行う

### 2. Rails の早期リターンパターン

**学んだこと:**
- コントローラーのアクションで複数の処理パターンがある場合、早期リターンパターンが有効
- 条件分岐をネストさせるよりも、早期リターンで処理を分離する方が可読性が高い

**良い例（今回の修正）:**
```ruby
def update
  return reset_keymap if params[:reset_to_default]
  return set_active_keymap if params[:active]
  return update_basic_info if basic_info_only?

  update_keymaps
end
```

**悪い例（修正前の潜在的な問題）:**
```ruby
def update
  keymaps_params = keymap_params  # reset時にエラー

  if params[:reset_to_default]
    reset_keymap
  elsif params[:active]
    set_active_keymap
  else
    update_keymaps
  end
end
```

### 3. マイグレーションのロールバック対応

**学んだこと:**
- マイグレーション失敗時は、本番環境で `kamal app exec --roles=web 'bin/rails db:rollback:primary STEP=1'` を使用
- Rails 8.1 では複数データベース対応のため、`db:rollback:primary` のように DB を指定する必要がある
- ロールバック後、修正したマイグレーションで再デプロイすればOK

### 4. Kamal のヘルスチェックタイムアウト

**学んだこと:**
- Kamal のデフォルトヘルスチェックタイムアウトは30秒
- SolidQueue などのバックグラウンドジョブ処理の起動に時間がかかる場合、タイムアウトエラーが発生する可能性
- 今回は1回目のデプロイでタイムアウトしたが、マイグレーション自体は成功していたため、2回目のデプロイで正常に起動

**対策案（将来的に検討）:**
- `config/deploy.yml` で `deploy_timeout` を調整
- ヘルスチェックエンドポイント（`/up`）の応答速度を改善
- バックグラウンドジョブの起動を非同期化

### 5. Turbo Frames によるタブ切り替えの実装

**学んだこと:**
- Turbo Frames を使うことで、ページ全体をリロードせずにタブコンテンツを切り替えられる
- サーバーサイドレンダリングなので、レッスン数が増えても初期ロードが軽い
- Stimulus Controller と組み合わせることで、スムーズなUX を実現

**重要なポイント:**
- `turbo_frame_tag` の ID を一致させる必要がある（partial 内でも同じ ID で wrap）
- レッスンリンクなどで Turbo Frame を抜ける場合は `data: { turbo_frame: "_top" }` を指定
- Stimulus Controller の `data-controller` を適切な親要素に配置（全ターゲットを包含する必要がある）

**設計方針:**
- クライアントサイド（Stimulus）よりサーバーサイド（Turbo Frames）を選択した理由:
  - レッスン数が増えても初期ロードが軽い
  - SEO にも有利（サーバーサイドレンダリング）
  - 実装がシンプル

### 6. ユーザー種別による表示制御

**学んだこと:**
- 管理者と一般ユーザーで表示するタブを分ける設計が有効
- `User#admin?` メソッドは環境変数ベースなので、スコープでは使えない
- コントローラー側で条件分岐し、適切なデータを渡すことで対応

**設計の教訓:**
- `Lesson.official` スコープが `users.admin` カラムを参照していたが、実際には `admin?` はメソッド
- スコープではなく、コントローラー側で `current_user.lessons` を使うことで解決
- 「管理者が作成したレッスン = 公式レッスン」という前提で設計がシンプルになった

## 明日の予定

- Day 22 完了: 本番環境へのデプロイ
  - 本番環境でマイグレーション実行（tab カラム追加）
  - タブ化機能の動作確認
- Day 23: 成績評価システムの実装開始
  - 4段階評価（プロ級、上級者、中級者、初心者）
  - 結果シェア機能の検討
- Day 24-25: 最終調整
  - パフォーマンス最適化
  - バグ修正
  - ドキュメント整備

## 所感

**Day 22 の成果:**
- キーマップ選択機能、タブ化機能と、2つの大きな機能を実装できた
- ユーザーフィードバックに基づいた改善を着実に進められている
- 本番環境でのマイグレーションエラー対応など、実践的な経験を積めた

**本番環境でのトラブル対応:**
- デプロイ時のマイグレーションエラーは初めての経験だったが、ログを読み解いて原因を特定し、適切に修正できた
- エラーメッセージ（`PG::NotNullViolation`）から「NULL値が存在する」ことを理解し、マイグレーション修正で対応
- ロールバック → 修正 → 再デプロイの一連の流れをスムーズに実行できた

**キーマップ選択機能の実装:**
- 複数キーマップ管理の基盤（Day 18-19 で実装）が活きてきた
- `active_keymap_set_id` という明示的なフラグを持つことで、ユーザーがどのキーマップを使用中か明確になった
- UI も分かりやすく、「使用中」バッジと「使用する」ボタンで直感的に操作できる

**タブ化機能の実装:**
- Turbo Frames + Stimulus による快適なタブ切り替えを実現
- トップページとレッスン管理画面の両方で統一されたUX
- 管理者と一般ユーザーで適切に表示を分けることで、それぞれの用途に最適化
- レッスン数が増えても見やすく、ユーザビリティが大幅に向上

**技術的な学び:**
- Turbo Frames のサーバーサイドレンダリングによる実装
- Stimulus Controller との組み合わせ
- `turbo_frame_tag` の正しい使い方（partial 内でも同じ ID で wrap）
- ユーザー種別による表示制御の設計

**今後の展望:**
- タブ化により、レッスンの整理がしやすくなった
- 将来的に「マイレッスン」「コミュニティ」タブを実装する準備が整った
- 次は成績評価システムや結果シェア機能など、よりユーザー体験を向上させる機能に取り組みたい
- Day 25 までのゴールに向けて、着実に進捗している
