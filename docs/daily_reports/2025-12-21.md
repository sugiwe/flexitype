# Day 21: レッスンDB化 Part 1 完了 - YAMLからPostgreSQLへの移行とアーキテクチャ改善

**日付**: 2025年12月21日
**作業時間**: 約6時間
**ブランチ**: feature/lesson-db-migration

---

## 📋 本日の目標

- レッスンデータのDB化（YAML → PostgreSQL）
- Category・Lessonモデルの設計・作成
- データ移行スクリプトの実装
- ユーザー用レッスンCRUD機能の実装
- サイドバーメニューへの課金ユーザー向けメニュー追加

---

## ✅ 完了したタスク

### 1. Category・Lessonモデルの設計と作成

**Categoryモデル**:
- カラム: name, description, display_order, requires_login, premium
- バリデーション: name必須、一意性制約
- スコープ: ordered（表示順でソート）、free（無料カテゴリー）

**Lessonモデル**:
- カラム: user_id, category_id, name, description, items (JSONB), count, is_public, requires_login, premium
- 関連: belongs_to :user, belongs_to :category, has_many :lesson_records
- 重要なスコープ実装:
  - `official`: 管理者が作成した公式レッスン
  - `user_created`: 一般ユーザーが作成したレッスン
  - `published`: 公開レッスン
  - `visible_to(user)`: ユーザーごとの閲覧可能レッスン（公式 + 自分 + 公開）
- `to_lesson_info`メソッド: JavaScript用のJSON形式変換

**設計上の決定**:
- 当初検討していた`lesson_type`フィールドは削除
  - 理由: カテゴリーとレッスン名で十分区別可能、シンプルさを優先
- items カラムは JSONB 型で配列を保存
  - フォームでは改行区切りのテキストエリアで入力

### 2. データ移行スクリプトの実装

**lib/tasks/lessons.rake**:
- YAML形式のレッスンデータ（`config/typing_lessons.yml`）をPostgreSQLに移行
- 公式ユーザー（typnix.app@gmail.com）に紐付けて作成
- 移行結果: 6カテゴリー、16レッスン

**技術的な課題と解決**:
1. **Duplicate index エラー**: `t.references :category` が自動的にインデックスを作成するため、手動の `add_index` が重複
   - 解決: 手動インデックス削除
2. **日本語parameterize問題**: `"基礎練習".parameterize` が空文字列を返す
   - 解決: カテゴリーキーを `"category_#{category.id}"` に変更

### 3. LessonLoaderサービスオブジェクトの削除とリファクタリング

**背景**:
- LessonLoaderはYAMLデータを変換するためのサービスオブジェクトだった
- DB化により不要になったため、よりRails wayなアプローチに変更

**変更内容**:
- `app/services/lesson_loader.rb` 削除
- `HomeController#index`: 直接ActiveRecordでカテゴリー・レッスン取得
- `LessonsController#show`: `@lesson.to_lesson_info` でJavaScript用データ生成

**メリット**:
- コードがシンプルになった
- Rails標準パターンに沿った設計
- RESTfulな設計に近づいた

### 4. ユーザー用レッスンCRUD実装（ハイブリッドアプローチ）

**設計の決定**:
- 当初は `/admin/lessons` と `/my/lessons` を分ける予定だった
- ユーザーからの提案: 公式アカウント（管理者）も課金ユーザーの一種として扱い、`/my/lessons` に統一
- メリット:
  - DRY原則に沿う
  - 将来的なサブスクリプション機能実装時の拡張性が高い
  - コードがシンプル

**My::LessonsController**:
- アクション: index, new, create, edit, update, destroy
- `visible_to` スコープで権限管理:
  - 管理者: 全レッスン閲覧可能
  - 一般ユーザー: 公式レッスン + 自分のレッスン + 公開レッスン
- Strong Parametersで権限別のパラメータ許可:
  - 管理者: premium, requires_login フラグも編集可能
  - 一般ユーザー: 常に無料・非公開に制限
- items の変換処理: テキストエリアの改行区切り → 配列

**ビュー実装**:
- `/my/lessons/index.html.slim`: カード形式のレッスン一覧
  - 公式バッジ表示
  - 自分のレッスンまたは管理者の場合に編集・削除ボタン
- `/my/lessons/_form.html.slim`: 共通フォームパーシャル
  - 管理者のみ requires_login, premium チェックボックス表示

### 5. Userモデルへの課金関連メソッド追加

```ruby
def premium?
  # 現時点では管理者のみtrue（仮実装）
  # 将来的にはsubscriptionテーブルを参照
  admin?
end

def premium_or_admin?
  premium? || admin?
end
```

**目的**:
- 将来的なサブスクリプション機能実装の基盤
- サイドバーメニューの表示制御

### 6. サイドバーナビゲーションの更新

**課金ユーザー向けメニュー追加**:
- 「レッスン管理」リンク
- `current_user.premium_or_admin?` で表示制御
- 紫テーマでスタイリング（一般メニューと差別化）

**管理者向けメニュー追加**:
- 「カテゴリー管理（準備中）」
- 無効化状態（cursor-not-allowed、opacity-60）
- 「準備中」バッジ表示
- 次回以降の実装予定機能

---

## 🔍 技術的な学び

### 1. JSONB型の活用

PostgreSQLのJSONB型を使うことで、配列データを柔軟に保存できた。フォームとの連携では、改行区切りテキストと配列の相互変換が必要。

```ruby
# Strong Parametersでの変換処理
if permitted_params[:items].is_a?(String)
  permitted_params[:items] = permitted_params[:items].split("\n").map(&:strip).reject(&:blank?)
end
```

### 2. ActiveRecordスコープの活用

権限管理を `visible_to` スコープに集約することで、コントローラがシンプルになった。

```ruby
scope :visible_to, ->(user) {
  if user&.admin?
    all
  else
    left_joins(:user).where(
      "lessons.user_id = :user_id OR lessons.is_public = true OR users.admin = true",
      user_id: user&.id
    ).distinct
  end
}
```

### 3. Rails wayなアーキテクチャ

サービスオブジェクトを削除し、モデル・コントローラ・ビューの責務を明確に分離することで、保守性が向上した。

### 4. ハイブリッドアプローチの有効性

管理者と一般ユーザーで別々のCRUDを作るのではなく、権限チェックで動作を分岐させる設計により:
- コードの重複を削減
- 将来的な拡張が容易
- テストがシンプル

---

## 🐛 遭遇したエラーと解決策

### 1. Duplicate index エラー

**エラー内容**:
```
PG::DuplicateTable: ERROR: relation "index_lessons_on_category_id" already exists
```

**原因**: `t.references :category` が自動的にインデックスを作成するのに、手動で `add_index :lessons, :category_id` を追加していた

**解決**: 手動インデックスを削除

### 2. 日本語parameterizeの問題

**問題**: `"基礎練習".parameterize.underscore` が空文字列を返し、全カテゴリーが同じキーになってしまった

**解決**: カテゴリーキーを `"category_#{category.id}"` に変更（安定したユニークキー）

### 3. User has_many :lessons の欠落

**エラー内容**:
```
NoMethodError: undefined method 'lessons' for an instance of User
```

**原因**: Userモデルに `has_many :lessons` が定義されていなかった

**解決**: 関連を追加

---

## 💭 設計上の議論と決定

### 1. lesson_typeフィールドの是非

**議論**: 単語・文章・文字練習を区別するために `lesson_type` カラムを用意すべきか？

**決定**: 削除する
- 理由: カテゴリー + レッスン名で十分区別可能
- シンプルさを優先

### 2. LessonLoaderの必要性

**議論**: サービスオブジェクトを残すべきか、削除してRails wayにすべきか？

**決定**: 削除してRails wayに
- YAMLデータ変換という目的がDB化により消滅
- ActiveRecordの標準パターンで十分対応可能
- RESTfulな設計に近づく

### 3. 管理者用CRUDとユーザー用CRUDの統合

**議論**: `/admin/lessons` と `/my/lessons` を分けるか、統合するか？

**決定**: `/my/lessons` に統合（ハイブリッドアプローチ）
- 公式アカウント（管理者）を課金ユーザーの一種として扱う
- 権限チェックで動作を分岐
- DRY原則に沿い、拡張性が高い

---

## 📊 コミット履歴

1. `レッスンDB化 Part 1: YAMLからPostgreSQLへの移行完了`
   - Category・Lessonモデル作成
   - データ移行スクリプト実装
   - 公式ユーザー作成（typnix.app@gmail.com）

2. `LessonLoaderを削除してRails wayなリファクタリングを完了`
   - サービスオブジェクト削除
   - HomeController、LessonsControllerをActiveRecord直接使用に変更

3. `ユーザー用レッスンCRUD実装（/my/lessons）`
   - My::LessonsController実装
   - レッスン一覧・作成・編集・削除機能
   - visible_toスコープによる権限管理

4. `サイドバーメニューに課金ユーザー向け・管理者向けメニューを追加`
   - premium?, premium_or_admin? メソッド追加
   - 「レッスン管理」メニュー（課金ユーザー向け）
   - 「カテゴリー管理（準備中）」メニュー（管理者向け）

---

## 🎯 次回のタスク（Day 22予定）

### カテゴリー管理機能の実装

**Admin::CategoriesController**:
- カテゴリーのCRUD操作
- 表示順の変更
- requires_login, premium フラグの設定

**ビュー実装**:
- `/admin/categories` - カテゴリー一覧
- カテゴリーの作成・編集・削除フォーム
- ドラッグ&ドロップでの表示順変更（Stimulus）

### その他の検討事項

- トップページのタブ化準備
- Google Analytics確認（GTM導入済み）
- パフォーマンス最適化（N+1クエリチェック）

---

## 🎓 今日の成果

レッスンのDB化により、以下が実現できました:

1. **柔軟なデータ管理**: YAMLファイルからPostgreSQLへの移行により、動的なレッスン作成が可能に
2. **シンプルなアーキテクチャ**: サービスオブジェクトを削除し、Rails標準パターンに統一
3. **拡張性の高い設計**: ハイブリッドアプローチにより、将来的なサブスクリプション機能実装の基盤を構築
4. **ユーザー体験の向上**: レッスン管理UIの実装により、管理者・課金ユーザーがWebから直接レッスンを作成・編集可能に

明日は管理者向けのカテゴリー管理機能を実装し、レッスンDB化の完成を目指します！
