# Google AdSense 導入設計

**実装ステータス**: ✅ **Day 20でサイト所有権確認完了**（審査リクエスト済み、審査中）

**最終更新**: 2025-12-20

---

## 📋 目次

1. [概要](#概要)
2. [実装フェーズ](#実装フェーズ)
3. [サイト所有権確認（完了）](#サイト所有権確認完了)
4. [審査プロセス（進行中）](#審査プロセス進行中)
5. [広告配置設計（審査通過後）](#広告配置設計審査通過後)
6. [広告の種類の制御](#広告の種類の制御)
7. [CMP（Cookie同意管理）](#cmpcookie同意管理)
8. [将来的な拡張](#将来的な拡張)

---

## 概要

### 目的

- サービス運営を支えるための広告収益化
- 固定サイズのディスプレイ広告を特定の位置に配置
- ユーザー体験を損なわない広告配置

### 基本方針

1. **広告配置の明確化**
   - 「この位置にこのサイズで入れる」を指定
   - サイドバー下部に300x250の固定サイズ広告
   - 将来的にヘッダー/フッターに横長広告も検討

2. **広告の種類の制御（部分的）**
   - IT/プログラミング関連の広告を優先
   - デリケートなカテゴリをブロック
   - 不適切な広告主を都度ブロック

3. **ユーザー体験の優先**
   - 広告が多すぎないように（初期は1〜2個）
   - デザインを崩さない配置
   - モバイルでも表示される広告のみ

---

## 実装フェーズ

### Phase 1: サイト所有権確認（✅ Day 20完了）

**実施内容**:
1. メタタグの追加
2. 環境変数設定
3. 本番環境デプロイ
4. サイト所有権確認成功

### Phase 2: 審査リクエスト（✅ Day 20完了）

**実施内容**:
1. CMPの設定を「後で通知」でスキップ
2. AdSense審査リクエスト完了

**審査期間**: 1〜2週間（通常）

### Phase 3: 広告配置（審査通過後）

**実施予定**:
1. ディスプレイ広告ユニット作成（300x250）
2. サイドバーに広告コードを追加
3. CSP設定を更新
4. 本番環境にデプロイ
5. 広告表示確認

---

## サイト所有権確認（完了）

### 実装内容

#### 1. メタタグの追加

**ファイル**: `app/views/layouts/application.html.slim`

**実装**:
```slim
/ Google AdSense（サイト所有権確認）
- if ENV['GOOGLE_ADSENSE_PUBLISHER_ID'].present?
  meta name="google-adsense-account" content=ENV['GOOGLE_ADSENSE_PUBLISHER_ID']
```

**特徴**:
- 環境変数で制御（本番環境のみ表示）
- 開発環境では表示されない（環境変数未設定）

#### 2. 環境変数設定

**ファイル**: `config/deploy.yml`

**追加内容**:
```yaml
env:
  secret:
    - RAILS_MASTER_KEY
    - FLEXITYPE_DATABASE_PASSWORD
    - ADMIN_EMAILS
    - ALLOWED_EMAILS
    - GTM_CONTAINER_ID
    - GOOGLE_ADSENSE_PUBLISHER_ID  # ← 追加
```

**ファイル**: `.kamal/secrets`

```bash
# Google AdSense パブリッシャーID
# サイト所有権確認用のメタタグに使用
GOOGLE_ADSENSE_PUBLISHER_ID=ca-pub-4593744795716641
```

**ファイル**: `.kamal/secrets.example`

```bash
# Google AdSense パブリッシャーID
# サイト所有権確認用のメタタグに使用
GOOGLE_ADSENSE_PUBLISHER_ID=ca-pub-XXXXXXXXXXXXXXXX
```

#### 3. 本番環境デプロイ

**実施内容**:
1. 初回デプロイ（メタタグ追加）
2. 環境変数設定漏れを修正（`config/deploy.yml` に追加）
3. 再デプロイ
4. Google AdSenseコンソールでサイト所有権確認成功

**学び**:
- `.kamal/secrets` に環境変数を追加しただけでは不十分
- `config/deploy.yml` の `env.secret` セクションにも追加が必要
- 環境変数が本番環境のコンテナに注入されるようになる

---

## 審査プロセス（進行中）

### 審査ステータス

**現在**: 審査中（2025-12-20にリクエスト）

**予想期間**: 1〜2週間（早ければ数日）

### 審査のポイント

**✅ クリア済み**:
- オリジナルコンテンツがある（練習機能、About、利用規約、プライバシーポリシー）
- ユーザーに価値を提供している
- 必須ページがある（プライバシーポリシー、利用規約）
- ナビゲーションがわかりやすい
- Cookie使用についてプライバシーポリシーに明記

**審査中の注意点**:
- 審査中は広告コードを貼らなくてOK
- サイトの大幅な変更は避ける（軽微な修正はOK）
- 審査結果がメールで通知される

### 審査通過後のステップ

1. AdSenseコンソールで「広告」→「広告ユニットごと」を選択
2. ディスプレイ広告ユニットを作成
3. 広告コードを取得
4. 新しいブランチ（`feature/google-adsense-ads`）を作成
5. 広告コードを実装
6. デプロイして広告表示を確認

---

## 広告配置設計（審査通過後）

### 広告タイプの選択

**ディスプレイ広告（固定サイズ）✅ おすすめ**

**特徴**:
- 特定の位置に固定サイズで配置できる
- サイズを指定可能（300x250、728x90など）
- デザインの一部として扱える

**自動広告（非推奨）**:
- Googleが自動的に広告を配置
- サイズや位置を制御できない
- デザインが崩れる可能性

### Phase 1: サイドバー広告（300x250）

#### 配置位置

**ファイル**: `app/views/layouts/shared/_sidebar.html.slim`

**実装イメージ**:
```slim
/ 広告スペース（サイドバー下部）
.mb-6
  / Google AdSense 広告
  - if ENV['GOOGLE_ADSENSE_ENABLED'] == 'true'
    ins.adsbygoogle style="display:inline-block;width:300px;height:250px" data-ad-client=ENV['GOOGLE_ADSENSE_PUBLISHER_ID'] data-ad-slot="YOUR_AD_SLOT_ID"
    javascript:
      (adsbygoogle = window.adsbygoogle || []).push({});
  - else
    / プレースホルダー（開発環境用）
    .bg-gray-200.dark:bg-gray-700.rounded-lg.flex.items-center.justify-center class="h-[250px] w-[300px]"
      p.text-sm.text-gray-500.dark:text-gray-400 広告スペース（300x250）
```

#### 環境変数の追加

**ファイル**: `.kamal/secrets`

```bash
# Google AdSense 広告表示フラグ
GOOGLE_ADSENSE_ENABLED=true

# Google AdSense 広告スロットID（サイドバー300x250）
GOOGLE_ADSENSE_SLOT_ID_SIDEBAR=1234567890
```

**ファイル**: `config/deploy.yml`

```yaml
env:
  secret:
    - RAILS_MASTER_KEY
    - FLEXITYPE_DATABASE_PASSWORD
    - ADMIN_EMAILS
    - ALLOWED_EMAILS
    - GTM_CONTAINER_ID
    - GOOGLE_ADSENSE_PUBLISHER_ID
    - GOOGLE_ADSENSE_ENABLED        # ← 追加
    - GOOGLE_ADSENSE_SLOT_ID_SIDEBAR # ← 追加
```

#### CSP設定の更新

**ファイル**: `config/initializers/content_security_policy.rb`

**追加内容**:
```ruby
Rails.application.config.content_security_policy do |policy|
  # 既存の設定...

  # Google AdSense
  policy.script_src :self, :https, :unsafe_inline,
    "https://accounts.google.com",
    "https://pagead2.googlesyndication.com",  # AdSense広告
    "https://adsbygoogle.js"                   # AdSense広告JS

  policy.frame_src :self, :https,
    "https://accounts.google.com",
    "https://googleads.g.doubleclick.net"      # AdSense広告iframe
end
```

### Phase 2: ヘッダー/フッター広告（将来実装）

#### 配置候補

**ヘッダー広告（728x90）**:
- コンテンツエリア直下
- 高いクリック率が期待できる
- モバイルではレスポンシブ広告に切り替え

**フッター広告（728x90）**:
- コンテンツエリア最下部
- ユーザー体験への影響が少ない

#### 広告数の推奨

- **初期**: 1〜2個（サイドバー1個）
- **最適化後**: 3〜4個（サイドバー + ヘッダー + フッター）
- ⚠️ 広告が多すぎるとユーザー体験が悪化 → 離脱率が上がる

---

## 広告の種類の制御

### 制御方法の概要

**結論**: 広告の種類を完全に指定することはできないが、いくつかの制御方法がある

### 方法1: デリケートなカテゴリのブロック

**設定場所**: AdSenseコンソール → 「ブランド保護」→「デリケートなカテゴリ」

**ブロックできるカテゴリ（一部）**:
- ギャンブル、宝くじ
- 出会い系
- 暴力的なコンテンツ
- アルコール
- ヘルスケア、医薬品

**注意**: コスメやゲームは「デリケートなカテゴリ」に含まれないため、完全にブロックはできない

### 方法2: 広告主のURLをブロック

**設定場所**: AdSenseコンソール → 「ブランド保護」→「広告主のURL」

**手順**:
1. 広告をクリック → 「この広告について」→ URL確認
2. AdSenseコンソールでそのURLをブロック

**運用**:
- 不適切な広告が表示されたら、都度ブロックする
- 継続的な運用が必要

### 方法3: ターゲティングの最適化（間接的）

**サイトのコンテンツで誘導**:
- サイトのコンテンツがIT/プログラミング/キーボード関連
- Googleは自動的にコンテンツを解析し、関連性の高い広告を配信

**メタデータの最適化**:
- OGP、meta descriptionでキーワードを明確に（既に実装済み）
- 「垂直配列キーボード」「タイピング練習」「プログラミング」などのキーワード

### 現実的な対応

1. **デリケートなカテゴリをブロック**（ギャンブル、出会い系など）
2. **不適切な広告が出たら都度ブロック**（広告主URL）
3. **サイトコンテンツを充実させる**（IT/プログラミング関連の記事など）
4. **しばらく様子を見る**（Googleが学習して最適化される）

**期待値**: 80-90%は希望に近い広告が表示されるようになる

---

## CMP（Cookie同意管理）

### 概要

**CMP（Consent Management Platform）**:
- EEA（欧州経済領域）、イギリス、スイスのユーザーに対して、Cookieやデータ収集の同意を取るための仕組み
- GDPR（EU一般データ保護規則）への対応

### 現在の対応

**審査リクエスト時の選択**: 「後で通知」でスキップ

**理由**:
1. 審査への影響なし（必須ではない）
2. 日本人ユーザーが大半（EEA/UK/スイスからのアクセスは極めて少ない）
3. 設定は後からでもOK

### 将来的な対応

#### 設定するタイミング

以下のいずれかに該当したら設定:
1. EEA/UK/スイスからのアクセスが全体の5%以上
2. AdSense収益が月1万円以上
3. AdSenseから設定を促す通知が来た

#### 推奨設定

**①2つの選択肢（「同意する」「オプションを管理する」）**

**理由**:
- GDPR対応として十分
- 広告収入への影響が小さい
- 無料で簡単に設定できる
- 日本のサイトとしては標準的な対応

**設定手順**（審査通過後）:
1. AdSenseコンソール → 「プライバシーとメッセージ」
2. 「2つの選択肢（「同意する」「オプションを管理する」）」を選択
3. メッセージのデザインをカスタマイズ（色、位置など）
4. 保存 → 自動的に全ページに表示される

---

## 将来的な拡張

### 収益化のベストプラクティス

#### 広告配置のコツ

**効果的な位置（クリック率が高い）**:
1. **サイドバー上部**（300x250）← 最も効果的
2. **コンテンツ直下**（728x90、レスポンシブ）
3. **記事中間**（336x280）

**現在の配置（サイドバー下部）**:
- クリック率は中程度
- デザインを崩さず、ユーザー体験を損なわない
- 最初はこれでOK、後で最適化可能

#### レポートと最適化

**AdSenseレポートで効果測定**:
- クリック率（CTR）
- インプレッション数
- 収益額
- ページRPM（1000インプレッションあたりの収益）

**最適化のサイクル**:
1. 1ヶ月運用して効果測定
2. クリック率が低い場合、配置を調整
3. 追加の広告ユニットを検討（ヘッダー、フッター）
4. 不適切な広告をブロック

### 追加の広告フォーマット

#### レスポンシブ広告

**特徴**:
- デバイスに応じて自動的にサイズが変わる
- モバイル対応が簡単
- レイアウトに柔軟に対応

**用途**:
- ヘッダー/フッター広告
- コンテンツ内広告

#### インフィード広告

**特徴**:
- コンテンツの一部として表示される
- ネイティブ広告の一種
- ユーザー体験を損なわない

**用途**:
- 練習一覧ページ（レッスン間）
- 履歴一覧ページ

---

## まとめ

### 実装完了（Day 20）

- ✅ サイト所有権確認メタタグの追加
- ✅ 環境変数設定（`GOOGLE_ADSENSE_PUBLISHER_ID`）
- ✅ 本番環境デプロイ
- ✅ サイト所有権確認成功
- ✅ AdSense審査リクエスト完了
- ✅ CMPは「後で通知」でスキップ

### 審査中（現在）

- 審査期間: 1〜2週間
- 審査中は広告コードを貼らなくてOK
- サイトの大幅な変更は避ける

### 審査通過後

1. ディスプレイ広告ユニット作成（300x250）
2. サイドバーに広告コードを追加
3. CSP設定を更新
4. 環境変数 `GOOGLE_ADSENSE_ENABLED` を追加
5. 本番環境にデプロイ
6. 広告表示を確認

### 将来的な拡張

- ヘッダー/フッター広告の追加
- レスポンシブ広告の導入
- インフィード広告の検討
- CMPの設定（必要に応じて）
